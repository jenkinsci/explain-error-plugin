<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form" xmlns:st="jelly:stapler">
    <l:layout title="AI Error Explanation">
        <st:include it="${it.run}" page="sidepanel.jelly"/>
        <l:main-panel>
            <h1>${%aiError(it.providerName)}</h1>
            
            <l:card title="Generated on: ${it.formattedTimestamp}">
                <pre style="white-space: pre-wrap; word-wrap: break-word;" class="jenkins-!-margin-bottom-0">${it.explanation}</pre>
                <div class="jenkins-!-margin-top-3">
                    <a id="explain-error-url" href="${it.urlString}" class="jenkins-button jenkins-!-destructive-color" target="_self" >
                        <l:icon src="symbol-warning-outline plugin-ionicons-api" class="icon-md" />
                        View failure output
                    </a>
                </div>
            </l:card>
            <j:if test="${it.hasSentPayloadAudit()}">
                <l:card title="Sanitized Payload Sent to AI">
                    <p>Redactions: ${it.redactionCount}; dropped by policy: ${it.droppedLineCount}; lines sent: ${it.inputLogLineCount}</p>
                    <pre style="white-space: pre-wrap; word-wrap: break-word;" class="jenkins-!-margin-bottom-0">${it.sentErrorLogs}</pre>
                </l:card>
            </j:if>
        </l:main-panel>
    </l:layout>
</j:jelly>
